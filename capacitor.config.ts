
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mumin.rehberi',
  appName: 'Mümin Rehberi',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
